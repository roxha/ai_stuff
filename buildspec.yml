version: 0.2
phases:
  build:
    commands:
      - echo "Bygger statisk app..."
      - mkdir -p dist
      - cp app/index.html dist/index.html
artifacts:
  base-directory: dist
  files:
    - '**/*'
