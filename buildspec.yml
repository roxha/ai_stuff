version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.9
      nodejs: 16
    commands:
      - echo "Installerer Python-avhengigheter..."
      - cd app && pip install -r requirements.txt
  build:
    commands:
      - echo "Build ferdig"
artifacts:
  files:
    - '**/*'
