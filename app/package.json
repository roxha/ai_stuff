{
  "name": "app",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p ../dist && cp index.html ../dist/index.html",
    "test": "echo 'No tests'",
    "start": "node index.js"
  },
  "dependencies": {}
}
