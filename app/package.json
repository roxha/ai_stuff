{
  "name": "app",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/ && (cp -r public dist/ || true) && echo 'Build success'",
    "install": "echo 'No install needed'"
  }
}
